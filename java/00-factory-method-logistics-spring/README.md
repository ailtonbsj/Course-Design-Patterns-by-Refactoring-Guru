# History of commands

```bash
# Project created
spring init --build=maven --java-version=17 --dependencies=web,devtools --groupId=br.ailtonbsj 00-factory-method-logistics-sample

# Run project
./mvnw spring-boot:run

# Navigate to project
xdg-open http://localhost:8080/
```